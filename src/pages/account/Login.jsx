import {useState} from "react"; import Page from "../../components/common/Page"; import Field from "../../components/common/Field"; import Logo from "../../components/layout/Logo";
export default function Login(){const[mode,setMode]=useState("login");return <Page narrow><div className="auth card"><div className="logo-center"><Logo/></div><div className="tabs"><button className={mode==="login"?"active":""} onClick={()=>setMode("login")}>Login</button><button className={mode==="signup"?"active":""} onClick={()=>setMode("signup")}>Create account</button></div><h1>{mode==="login"?"Welcome back":"Create your RentPartner account"}</h1><p className="muted">{mode==="login"?"Sign in to manage requests and your profile.":"Save your details for faster future bookings."}</p><Field label="Mobile or email"><input placeholder="you@example.com"/></Field><Field label="Password"><input type="password" placeholder="••••••••"/></Field><button className="btn full" onClick={()=>alert("Demo login — connect real authentication for production.")}>{mode==="login"?"Login":"Create account"}</button><p className="fine">Demo UI only. Real authentication requires a backend/auth provider.</p></div></Page>}
