import {useParams,Link} from "react-router-dom"; import Page from "../../components/common/Page"; import Icon from "../../components/common/Icon"; import {partners} from "../../data/partners";
export default function PartnerProfile(){const{id}=useParams();const p=partners.find(x=>x.id===Number(id));if(!p)return <Page narrow><h1>Partner not found</h1><Link className="btn" to="/partners">Back to partners</Link></Page>;return <Page><div className="profile"><div className="avatar xl">{p.name[0]}</div><div><div className="eyebrow">Verified partner</div><h1>{p.name} <span className="verified"><Icon name="BadgeCheck"/></span></h1><p className="location"><Icon name="MapPin"/> {p.city} · ★ {p.rating}</p><p className="lead">{p.intro}</p><Link className="btn" to="/book">Request {p.name} <Icon name="ArrowRight"/></Link></div></div><div className="content-grid"><div><h2>Services</h2><div className="mini-cards">{p.services.map(x=><div className="mini card" key={x}><Icon name="CheckCircle2"/><b>{x}</b></div>)}</div><h2 className="mt">About availability</h2><p>Availability shown here is illustrative. Submit a booking request with your preferred date and time so the team can check current availability.</p></div><div className="card side-card"><h3>Booking snapshot</h3><p><b>Starting price</b><br/>{p.price}</p><p><b>Verification</b><br/>Profile reviewed</p><p><b>Booking</b><br/>Request first, confirmation after availability check.</p></div></div></Page>}
