import {useParams,Link} from "react-router-dom";
import Page from "../../components/common/Page"; import Icon from "../../components/common/Icon"; import {services} from "../../data/services";
export default function ServiceDetail(){const {id}=useParams();const s=services.find(x=>x.id===id);if(!s)return <Page narrow><h1>Service not found</h1><Link className="btn" to="/services">View services</Link></Page>;return <Page><div className="detail-hero"><div className="service-icon big"><Icon name={s.icon}/></div><div><div className="eyebrow">Service</div><h1>{s.name}</h1><p className="lead">{s.desc}</p><div className="tag-row">{s.tags.map(t=><span key={t}>{t}</span>)}</div><Link className="btn" to={`/book?service=${s.id}`}>Request this service <Icon name="ArrowRight"/></Link></div></div><div className="content-grid"><div><h2>What to expect</h2><div className="check-list">{["Clear service scope before confirmation","Availability checked by the RentPartner team","Respectful conduct and privacy expectations","Support if you need help with a booking"].map(x=><p key={x}><Icon name="CheckCircle2"/>{x}</p>)}</div></div><div className="card side-card"><Icon name="ShieldCheck"/><h3>Safety first</h3><p>Review our safety guidelines before meeting a partner or starting a service.</p><Link to="/safety">Read safety guidelines <Icon name="ArrowRight"/></Link></div></div></Page>}
